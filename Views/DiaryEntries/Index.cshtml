@model List<DiaryEntry>

<div class="diary-entries-container">
    <div class="diary-topbar">
        <h5 class="fw-bold">Write Entries</h5>
        <a class="btn btn-primary mt-2" asp-controller="DiaryEntries" asp-action="WriteEntries">
            Create new Diary entry
        </a>
    </div>

    <h3 class="diary-heading">Your Diary Entries</h3>

    <div class="table-responsive diary-table-wrapper">
        <table class="table custom-diary-table">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Content</th>
                    <th>Date</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var entry in Model)
                {
                    <tr>
                        <td>@entry.Title</td>
                        <td>@entry.Content</td>
                        <td>@entry.CreatedAt.ToString("MMM dd, yyyy - hh:mm tt")</td>
                        <td>
                            <a class="entry-action" href="#">Edit</a> |
                            <a class="entry-action" href="#">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
